os_security_group_rule_defaults:
  - group_name: "{{ openstack_security_group_name }}"
    direction: egress
    ethertype: IPv6
  - group_name: "{{ openstack_security_group_name }}"
    direction: egress
    ethertype: IPv4
